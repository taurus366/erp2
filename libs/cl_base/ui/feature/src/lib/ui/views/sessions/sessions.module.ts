import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { RouterModule } from '@angular/router';
import { SessionsRoutes } from './sessions.routing';  // Make sure this is correct

@NgModule({
  imports: [
    // CommonModule,
    // RouterModule.forChild(SessionsRoutes)  // Set up routing for this module
  ],
  declarations: [
    // Your components here, for example

  ]
})
export class SessionsModule {}
